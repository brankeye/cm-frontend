<?xml version="1.0" encoding="utf-8" ?>
<base:ContentCore xmlns="http://xamarin.com/schemas/2014/forms"
           xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
           x:Class="cm.frontend.core.Phone.Views.Pages.Editors.EvaluationSection"
           xmlns:base="clr-namespace:cm.frontend.core.Phone.Views.Pages.Base;assembly=cm.frontend.core.Phone"
           xmlns:pages="clr-namespace:cm.frontend.core.Phone.ViewModels.Pages.Editors;assembly=cm.frontend.core.Phone"
           x:TypeArguments="pages:EvaluationSection">
  <ContentPage.Content>
    <StackLayout>
      <Label Text="Section name"></Label>
      <Entry Text="{Binding Name, Mode=TwoWay}"></Entry>
      <Label Text="Score"></Label>
      <Entry Text="{Binding Score, Mode=TwoWay}" Keyboard="Numeric"></Entry>
      <Label Text="Body"></Label>
      <Entry Text="{Binding Body, Mode=TwoWay}"></Entry>
      <StackLayout Orientation="Horizontal">
        <Button Text="Cancel" Command="{Binding CancelCommand}"></Button>
        <Button Text="Ok" Command="{Binding SaveCommand}"></Button>
      </StackLayout>
    </StackLayout>
  </ContentPage.Content>
</base:ContentCore>