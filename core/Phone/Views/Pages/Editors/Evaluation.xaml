<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
           xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
           x:Class="cm.frontend.core.Phone.Views.Pages.Editors.Evaluation"
           Title="Evaluation editor">
  <StackLayout Padding="15">
    <Label Text="Evaluation name" VerticalOptions="Center" />
    <Entry Text="{Binding Name, Mode=TwoWay}" VerticalOptions="Center"></Entry>
    <StackLayout Orientation="Horizontal">
      <Button Text="Cancel" Command="{Binding CancelCommand}" VerticalOptions="Center"></Button>
      <Button Text="Ok" Command="{Binding SaveCommand}" VerticalOptions="Center"></Button>
    </StackLayout>
  </StackLayout>
</ContentPage>