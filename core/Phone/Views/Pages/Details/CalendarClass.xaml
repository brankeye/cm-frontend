<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
           xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
           xmlns:controls="clr-namespace:cm.frontend.core.Phone.Views.Controls;assembly=cm.frontend.core.Phone"
           x:Class="cm.frontend.core.Phone.Views.Pages.Details.CalendarClass"
           Title="Class">
  <ContentPage.Content>
    <controls:PrettyListView ItemsSource="{Binding AttendingList}" Header="{Binding .}" HasUnevenRows="False" IsPullToRefreshEnabled="False" QuickSelect="True">
      <controls:PrettyListView.ItemTemplate>
        <DataTemplate>
          <ViewCell>
            <StackLayout Orientation="Horizontal" Padding="15,8,15,8">
              <Label Text="{Binding Attendance}" VerticalTextAlignment="Center"></Label>
              <Label Text="{Binding AttendingModel.Profile.FullName}" VerticalTextAlignment="Center"></Label>
            </StackLayout>
          </ViewCell>
        </DataTemplate>
      </controls:PrettyListView.ItemTemplate>
      <controls:PrettyListView.HeaderTemplate>
        <DataTemplate>
          <StackLayout Padding="15">
            <StackLayout Orientation="Horizontal">
              <Label Text="{Binding ClassModel.Name}" FontAttributes="Bold" VerticalTextAlignment="Center"></Label>
              <Label Text="(Canceled)" FontAttributes="Bold" VerticalTextAlignment="Center" IsVisible="{Binding IsCanceled}"></Label>
            </StackLayout>
            <StackLayout Orientation="Horizontal">
              <Label Text="Date: " VerticalTextAlignment="Center"></Label>
              <Label Text="{Binding Date, StringFormat='{0:dddd, MMMM d, yyyy}'}" VerticalTextAlignment="Center"></Label>
            </StackLayout>
            <StackLayout Orientation="Horizontal">
              <Label Text="Time: " VerticalTextAlignment="Center"></Label>
              <Label Text="{Binding ClassModel.StartTime, StringFormat='{0:h:mm tt}'}" VerticalTextAlignment="Center"></Label>
              <Label Text=" to " VerticalTextAlignment="Center"></Label>
              <Label Text="{Binding ClassModel.EndTime, StringFormat='{0:h:mm tt}'}" VerticalTextAlignment="Center"></Label>
            </StackLayout>
            <StackLayout Orientation="Horizontal" Spacing="0">
              <Label Text="Attending?" FontAttributes="Bold" VerticalTextAlignment="Center"></Label>
              <Button Text="Yes" Command="{Binding AttendingCommand}" CommandParameter="True" BorderRadius="0" VerticalOptions="Center" Margin="20,0,0,0"></Button>
              <Button Text="No" Command="{Binding AttendingCommand}" CommandParameter="False" BorderRadius="0" VerticalOptions="Center" Margin="20,0,0,0"></Button>
            </StackLayout>
            <StackLayout Orientation="Horizontal" Spacing="0" IsVisible="{Binding IsCanceledViewVisible}">
              <Label Text="Canceled?" FontAttributes="Bold" VerticalTextAlignment="Center"></Label>
              <Switch IsToggled="{Binding IsCanceled, Mode=TwoWay}" VerticalOptions="Center" Margin="22,0,0,0"></Switch>
            </StackLayout>
            <StackLayout.GestureRecognizers>
              <TapGestureRecognizer></TapGestureRecognizer>
            </StackLayout.GestureRecognizers>
          </StackLayout>
        </DataTemplate>
      </controls:PrettyListView.HeaderTemplate>
    </controls:PrettyListView>
  </ContentPage.Content>
</ContentPage>