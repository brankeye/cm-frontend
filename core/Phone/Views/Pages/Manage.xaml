<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="cm.frontend.core.Phone.Views.Pages.Manage"
             Navigation="{Binding Navigation, Mode=OneWayToSource}">
  <ContentPage.Content>
    <StackLayout>
      <Label Text="Manage" VerticalTextAlignment="Center"></Label>
      <Entry Placeholder="School name"></Entry>
      <StackLayout Orientation="Horizontal">
        <Button Text="Join" Command="{Binding JoinCommand}"></Button>
        <Button Text="Create" Command="{Binding CreateCommand}"></Button>
      </StackLayout>
    </StackLayout>
  </ContentPage.Content>
</ContentPage>